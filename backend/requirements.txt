# FastAPI and server
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Data validation
# Using newer versions with pre-built wheels to avoid Rust compilation issues
pydantic>=2.8.0
pydantic-settings>=2.4.0

# Vector database (FAISS)
# Using latest compatible version (1.7.4 is no longer available on PyPI)
# Version 1.9.0+ supports Python 3.11
faiss-cpu>=1.9.0
numpy>=1.24.3,<2.0.0

# Knowledge Graph (Neo4j)
neo4j==5.15.0

# HTTP client for API calls
httpx==0.25.2

# PDF processing
PyPDF2==3.0.1

# Logging and utilities
python-dotenv==1.0.0

